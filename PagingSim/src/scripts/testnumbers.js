export default function handleInputValues(vpn, offset) {
    console.log('VPN and Offset:', vpn + offset);
    console.log('Offset:', offset);
    // Do something with the vpn and offset values...
  }